*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/

# Local secrets / env — never commit. The template `.env.example` IS tracked.
.env
!.env.example

# Local platform config (may carry a real repo URL). The template is tracked.
platform.config.json

# Platform working dir (clones, worktrees, events.db) + the operator-set project config
# (holds the API key at rest, 0600). Never commit. (CYP-96)
.cyppie/
project-config.json
# Multi-project registry (N projects + active pointer), 0600 under the gitRoot. (CYP-91)
projects.json
# Cross-project channel-share gate, 0600 under the gitRoot. (CYP-93)
channel-shares.json
