<?xml version="1.0" encoding="utf-8"?>
<!-- Demo/test harness manifest — non-shipped. Launcher activity renders the operator-context
     EventLogDemoApp (stub sources) for the maestro/eventlog-*-android.yaml flows. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".DemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- CYP-47 DEBUG (temporary): instrumented Live-Tail harness for the Android connection-state
             root-cause. Launch: adb shell am start -n com.tneff.cyppieagents.demo/.TailDebugActivity -->
        <activity
            android:exported="true"
            android:name=".TailDebugActivity" />
    </application>

</manifest>
