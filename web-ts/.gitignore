# CYP-398 — web-ts (Node/Vite) build artifacts. node_modules is already ignored repo-wide.
node_modules
dist
dist-ssr
*.local
.vite

# CYP-399 — generated contract types ("generate, don't commit"; produced by `npm run contract:gen`)
src/types/generated/

# CYP-423 — generated maritime token CSS ("generate, don't commit"; produced by `npm run tokens:gen`)
src/ui/tokens.generated.css
