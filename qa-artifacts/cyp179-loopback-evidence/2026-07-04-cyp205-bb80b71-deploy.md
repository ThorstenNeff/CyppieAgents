# CYP-205 — Deploy `bb80b71` (Hub + SPA + CYP-196-atomic) LIVE (redacted)

**Datum:** 2026-07-04 · Auftraggeber-GO, outward-facing. NUR Hub-Jar + SPA + CYP-196-db/config. Caddy/Kratos PRESERVED.

## Prep (separater Worktree `cyp205-worktree-bb80b71`, main-Worktree=live unberührt)
- Hub-Jar `:server:shadowJar` bb80b71 (md5 6c04b324) → staged `.bb80b71-ready`, Backup `.ae60635-live.bak` (63ab8505).
- SPA `:app:webApp:jsBrowserDistribution` bb80b71 (webApp.js md5 1956f4ec, 0 `.map`, tokenlos) → **3 Auth-Globals re-injiziert** (CYP-195-Gotcha: frischer Build hat sie NICHT). Backup `spa-dist.ae60635-bundle.bak`.
- `platform.config.json` (Backup `.pre-cyp196.bak`): `auth.bootstrapOperatorIdentityId=ab7c54e3-2f27-46f4-ab5e-0b881abab07d`.

## CYP-196 ATOMIC-Apply (Test-Ordering, Privilege-Escalation)
Hub-DOWN (`launchctl bootout`, KeepAlive=true → bleibt down) → [down] Jar-Swap bb80b71 + `DELETE role_assignments WHERE identity_id='7e247283-…'` → Hub-UP (`launchctl bootstrap`). Slot-frei NUR während down. NICHT stale-Row auf altem Jar.

## Verify (Loopback + extern, RC2)
- Hub bb80b71 health 200 · register enum-safe · ACL-ghost→404 · **/ws/hub→101**.
- **CYP-196:** random verified→**MEMBER** (workspace/members·audit·projects→403 = land-grab zu) · break-glass OPERATOR_TOKEN→200 · OPERATOR-count 0 (nur ab7c54e3-Pin bei Login). `ab7c54e3`=OPERATOR config-gepinnt (v1.3.0 kein admin-session-mint → Auftraggeber-Login-confirm).
- **CYP-198/204:** `/ws/agent?since=0`→101 (Hub-WS-Replay).
- **CYP-195 preserved:** 3 Globals · resolveAuthMode→Live · tokenlos · served-md5==built (1956f4ec).
- **Belt-off:** /→200 · events→401 · admin→404 · MUST-1→404 · .git/.map→404 · /ws/hub→Hub.
- **CYP-203 preserved:** apex/www→401-ohne/200-mit · app-ungated. **CYP-201 HSTS** max-age=259200. Kein Secret im Bundle (0).
- **PRESERVED:** Caddy + Kratos unangetastet.

## ⚠️ Ehrlicher Flag (nicht-fatal)
`hub.err.log` beim bb80b71-Boot: `NoClassDefFoundError: io.netty.buffer.FreeChunkEvent` (20×). Hub voll funktional (WS-101 beweist netty wirkt) → benign Log-Noise (wahrsch. netty-Version-Mismatch im Build). An Backend flaggen.

## Persistenz / Rollback
Jar+SPA+config+roles.db auf Live-Pfaden → reboot-fest. Rollback: Jar←`.ae60635-live.bak` + SPA←`.ae60635-bundle.bak` + config←`.pre-cyp196.bak` (+ stale-Row-Restore falls CYP-196-Revert nötig) + Hub-Restart.
