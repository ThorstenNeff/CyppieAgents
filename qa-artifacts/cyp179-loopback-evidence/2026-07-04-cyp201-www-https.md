# CYP-201 — www.cyppie-agents.com HTTPS-only (redacted)

**Datum:** 2026-07-04 · **Auftraggeber-Priorität, human-gated GO.** Caddy-Edge.

## Fix
- `Caddyfile.cyp173` (Plist-Pfad, Backup `.pre-cyp201-www.bak`): neuer Site-Block
  `www.cyppie-agents.com { redir https://cyppie-agents.com{uri} permanent }`.
- Caddy auto-provisioniert LE-Cert für www (DNS `www`→162.55.248.10 resolvte) + Auto-HTTP→HTTPS. Reload (zero-downtime).

## Verify (RC2)
- `http://www/` → **308** → `https://www…/` (kein Plain-HTTP-Serve).
- `https://www/` LE-Cert: **CN=www.cyppie-agents.com, Let's Encrypt** (valid).
- `https://www/` → **301** → `https://cyppie-agents.com/` (redirect-to-Apex).
- `https://www/docs` → **301** → `https://cyppie-agents.com/docs` (URI-preserving).
- Kein Browser-Warning.

## Keine Regression
apex `/`→200 · apex `/docs`→200 · interne `/ACL-MATRIX.md`→404 · app `/`→200 · `/api/auth/me`→200 · admin→404 · 301-alt-host `api.cyppie.com/x`→301. Alles unberührt.

## Persistenz / Rollback / offen
- Persistent (cyp173 = Plist-Pfad). Rollback: `cp Caddyfile.cyp173.pre-cyp201-www.bak Caddyfile.cyp173` + reload.
- **HSTS (offen, Bestätigung):** empfohlen `max-age=31536000; includeSubDomains` auf apex+www+api (konservativ erst 300). Browser-Commitment → separater GO.

## HSTS (Auftraggeber-Go, konservativ-first)
- `header Strict-Transport-Security "max-age=259200"` (72h, per-host, KEIN includeSubDomains) in api + apex + www Blöcken. Backup `Caddyfile.cyp173.pre-cyp201-hsts.bak`. Reload.
- **Verify:** HSTS-Header present auf `api.cyppie-agents.com` + `cyppie-agents.com` + `www.cyppie-agents.com` HTTPS-Responses (`max-age=259200`).
- **Keine Regression:** http→308→https · www→301→apex · apex/docs 200 · interne 404 · app/auth 200 · MUST-1/admin 404 · api-Cert unverändert.
- Commitment-Rückweg ≤ 72h (self-heal). **Folge (separater Go nach sauberem Lauf):** `includeSubDomains` + `max-age=31536000`.
